/// Identifies a settings sub-page. UI concerns (icon, label, content) live in
/// the `SettingCategoryPresentation` extension in the views layer.
enum SettingCategory { account, connection, preferences, qaToolbox }
