name: Auto Assign Task
on:
  push:
jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: Get the column ID
        run: |
          gh project field-list 28 --owner mysteriumnetwork --format json
        env:
          GH_TOKEN: ${{ secrets.GH_PROJECTS_RO_TOKEN }}
