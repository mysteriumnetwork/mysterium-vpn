allprojects {
    // Only apply this to the buildscript (where the plugins live)
    // This fixes the "403 Forbidden" download error
    buildscript {
        repositories {
            mavenCentral()
            google()
            gradlePluginPortal()
        }
    }
}